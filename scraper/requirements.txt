# requests + beautifulsoup4: consulta ao vivo em cbo.mte.gov.br (JSF com
# ViewState/sessão) pra saber se cada família exige formação profissional —
# art. 429 CLT. boto3 (SSM) já vem embutido no runtime Python da Lambda.
requests==2.34.2
beautifulsoup4==4.15.0
